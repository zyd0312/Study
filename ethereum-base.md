# 以太坊基础学习笔记：账户、状态、交易与智能合约

这份笔记整理以太坊底层基础概念，帮助理解：以太坊如何管理状态、如何运行智能合约、普通账户和合约账户有什么区别，以及普通转账和合约交互的流程。

## 1. 以太坊不是只记录交易，而是维护一个全局状态机

很多人一开始会把区块链理解为：

```text
收集交易 -> 打包交易 -> 出块 -> 节点共识 -> 继续出块
```

这个理解是对的，但还不完整。以太坊更准确地说是一个所有节点共同维护的状态机。

每个区块不只是保存一批交易，还代表一次状态转换：

```text
旧状态 State_N
  执行 Tx1
  执行 Tx2
  执行 Tx3
  ...
新状态 State_N+1
```

以太坊的状态来自：

```text
创世状态 + 所有历史区块里的所有交易按规则执行
```

所以以太坊不是简单的“交易日志系统”，而是“确定性状态转换系统”。

## 2. 以太坊状态是什么

以太坊全局状态可以粗略理解为：

```text
地址 -> 账户数据
```

每个账户包含：

```text
nonce
ETH balance
codeHash
storageRoot
```

账户分两类：

```text
EOA 普通账户：
- 由私钥控制
- 可以主动发交易
- 没有合约代码
- 通常没有合约 storage

Contract Account 合约账户：
- 没有私钥
- 有合约代码
- 有自己的 storage
- 只能被交易或其他合约调用后执行
```

比如 ERC20 合约中记录的 token 余额，不是存在用户普通账户里，而是存在 ERC20 合约账户的 storage 里。

## 3. 区块头里的 stateRoot

以太坊区块头中有一个关键字段：

```text
stateRoot
```

它是执行完这个区块所有交易后，全局状态的哈希承诺。

节点验证区块时，会从父区块状态开始，按区块内交易顺序重新执行一遍。如果节点算出的状态根和区块头里的 `stateRoot` 不一致，这个区块就是无效的。

这保证出块者不能随便改状态，比如不能凭空给自己加 ETH。

### 3.1 链上数据如何被查到

用户平时不是直接“问区块链网络查数据”，而是通过 JSON-RPC 问某个以太坊节点。节点同步区块、执行交易、维护本地状态数据库，然后按请求返回数据。

链上数据常见有几类：

```text
合约 storage：
比如 mapping、uint、bytes32。可以用 eth_getStorageAt 或 eth_call 读取。

交易 calldata：
交易输入数据，存在区块交易体里。可以通过交易 hash 或区块数据读取。

event logs：
合约 emit 的事件，存在交易 receipt 里。可以用 eth_getLogs 或 receipt 查询。

合约 bytecode：
合约账户的 runtime code。可以用 eth_getCode 读取。

blob 数据：
EIP-4844 后 Rollup 常用。L1 保存 commitment，blob 数据只保证短期可用。
```

以合约 storage 为例，节点查询某个 slot 时，底层路径大致是：

```text
区块头 stateRoot
  -> 全局 state trie
  -> 用 keccak256(address) 找到合约账户
  -> 合约账户里有 storageRoot
  -> 用 keccak256(slot) 找到具体 storage value
```

这里的 `keccak256(address)` 不是解密，而是把地址变成状态树里的查找路径。哈希输出的 32 字节会被拆成 64 个 nibble，每个 nibble 对应 Merkle Patricia Trie 中 0 到 f 的分支。

如果不信 RPC 节点，可以要求它返回 proof，例如 `eth_getProof`。验证 proof 时，本地会做两层检查：

```text
accountProof：
从区块头 stateRoot 出发，按 keccak256(address) 走 state trie，
检查每个节点 RLP 编码后的 hash 是否接到下一个节点，
最后确认这个合约账户和它的 storageRoot 属于该 stateRoot。

storageProof：
从账户的 storageRoot 出发，按 keccak256(slot) 走 storage trie，
同样逐层检查节点 hash，
最后确认这个 slot 的 value 属于该 storageRoot。
```

因此 proof 验证的核心不是重新执行交易，而是确认：

```text
这个 value 确实被某个区块头里的 stateRoot 承诺。
```

## 4. 谁运行智能合约

智能合约代码存储在以太坊链上状态里。合约部署后，它的 runtime bytecode 会成为某个合约账户的代码。

当一笔交易调用合约时：

```text
用户发交易到合约地址
  -> 交易被打包进区块
  -> 节点读取合约 bytecode
  -> EVM 执行合约代码
  -> 修改账户余额 / 合约 storage / 生成日志
  -> 得到新的 stateRoot
```

不是合约作者在运行合约，也不是某台中心化服务器在运行合约，而是所有验证该区块的以太坊执行节点都会按照 EVM 规则运行它。

## 5. 共识层和执行层

现在以太坊使用 PoS，不再是 PoW 挖矿。可以简化理解为：

```text
共识层：
- 决定谁提议区块
- 决定哪个区块属于 canonical chain
- 提供 finality

执行层：
- 执行交易
- 运行 EVM
- 更新账户状态
- 验证 stateRoot
```

一个区块要被接受，需要两件事都成立：

```text
共识层接受这个区块
执行层确认这个区块的状态转换合法
```

## 6. 普通账户是如何创建的

普通账户，也叫 EOA，创建过程不需要上链。

流程是：

```text
本地生成随机私钥
  -> 推导公钥
  -> 从公钥计算以太坊地址
```

也就是：

```text
随机数 -> 私钥 -> 公钥 -> 地址
```

这不需要交易、不需要 gas、不需要注册。只要你掌握私钥，就控制对应地址。

EOA 的特点：

```text
有私钥
可以主动发交易
没有代码
用签名证明控制权
```

### 6.1 随机性、地址空间和碰撞

EOA 私钥本质上是一个落在 secp256k1 曲线合法范围内的 256-bit 数字。钱包生成私钥时，应使用操作系统或浏览器提供的密码学安全随机数生成器。

地址生成可以简化为：

```text
private key
  -> secp256k1 public key
  -> keccak256(public key)
  -> 取后 20 bytes
  -> Ethereum address
```

如果生成私钥时的随机数被攻击者知道，或者随机性很差，攻击者就可能复现或枚举出私钥。真正危险的通常不是哈希碰撞，而是：

```text
弱随机
脑钱包口令太简单
助记词泄露
私钥被恶意软件读取
签名钓鱼
```

以太坊地址是 20 bytes，也就是 160 bit，所以理论地址空间是：

```text
2^160
```

这个空间有限，但大到现实中不可用尽。state trie 也不是预先存满所有地址，只有发生过链上状态变化的地址才会进入状态，例如有余额、nonce、code 或 storage。

不同私钥理论上可能映射到同一个地址，但自然碰撞概率极低。攻击者随机枚举私钥，确实有数学可能碰到有钱账户；但对于均匀随机生成的私钥，所需尝试次数远超现实计算能力。现实攻击更多是扫弱私钥、弱助记词或人的操作漏洞。

## 7. 智能合约是如何创建的

智能合约账户不是靠私钥创建的，而是通过合约部署交易创建的。

流程是：

```text
开发者写 Solidity 源码
  -> 编译成 EVM creation bytecode
  -> EOA 发部署交易
  -> 交易 to 字段为空
  -> data 字段包含 creation bytecode 和构造参数
  -> 交易被打包进区块
  -> EVM 执行 creation code
  -> 返回 runtime bytecode
  -> 以太坊创建合约账户并保存代码
```

部署后的合约账户有：

```text
address
code
storage
balance
nonce
```

但它没有私钥，不能主动发交易。它只能在被 EOA 或其他合约调用时执行。

## 8. 合约地址如何确定

普通 `CREATE` 部署方式下，合约地址由：

```text
部署者地址
部署者 nonce
```

决定。

可以粗略理解为：

```text
contract address = hash(deployer address, deployer nonce) 的后 20 字节
```

还有一种 `CREATE2`，地址由：

```text
部署者地址
salt
creation bytecode hash
```

决定。`CREATE2` 可以提前计算未来合约地址。

## 9. 普通账户能否随意部署合约

原则上可以。以太坊是 permissionless 的。

只要满足：

```text
有私钥能签名
账户有足够 ETH 支付 gas
部署交易符合协议规则
bytecode 没超过协议限制
```

普通账户就可以部署智能合约。

以太坊底层不会判断：

```text
这个合约是不是善意的
这个项目是不是骗局
这个业务是不是公平
```

协议层只判断交易和 EVM 执行是否合法。

## 10. 恶意智能合约和检查机制

恶意合约确实大量存在，例如：

```text
貔貅盘 / honeypot
钓鱼授权合约
假空投领取合约
伪装成知名项目的假合约
owner 可随意增发或冻结的 token
可升级代理后门
利用漏洞攻击其他协议的合约
```

但恶意合约不等于非法区块。只要它按 EVM 规则执行，以太坊协议层就会接受它。

检查和防护主要在应用层：

```text
区块浏览器源码验证
安全审计
钱包风险提示
交易模拟
安全公司黑名单
社区标记
DApp 前端过滤
协议内部白名单
```

核心安全原则是：

```text
坏合约可以被部署，
但不能绕过 EVM 和账户权限规则直接偷走你的资产。
```

通常它需要你授权、签名或交互，才有机会影响你的资产。

## 11. 普通账户直接在 L1 转账

如果 A 给 B 转 ETH，流程是：

```text
A 钱包构造交易
  -> to = B 地址
  -> value = 转账金额
  -> nonce
  -> gas 参数
  -> chainId = Ethereum mainnet
A 用私钥签名
  -> 广播到以太坊网络
  -> 进入 mempool
  -> 被打包进 L1 区块
  -> 所有节点执行交易
  -> A ETH 减少
  -> B ETH 增加
```

如果只是 ETH 转账，不需要智能合约。

如果转的是 ERC20 token，则需要调用 token 合约：

```text
USDC.transfer(B, amount)
```

因为 token 余额记录在 USDC 合约的 storage 里。

## 12. 能否绕过智能合约直接与以太坊交互

可以直接发 L1 交易，但不能绕过合约规则修改合约控制的状态。

例如：

```text
EOA -> EOA 的 ETH 转账：
可以不调用合约

从桥合约中取出 ETH：
必须调用桥合约，并满足合约逻辑

修改 ERC20 余额：
必须通过 ERC20 合约逻辑
```

你不能发一笔交易说“直接拿走某个合约里的 ETH”，因为合约账户没有私钥，合约余额只能通过合约代码允许的路径改变。

如果出块者试图跳过合约逻辑直接改状态，其他节点重新执行区块时会发现 `stateRoot` 不一致，并拒绝该区块。

## 13. 一句话总结

以太坊是一个确定性状态机。普通账户用私钥控制，智能合约账户由部署交易创建并把代码存在链上。交易触发状态转换，合约调用由 EVM 执行，所有节点复算并验证最终 `stateRoot`。以太坊底层保证“执行规则正确”，但不保证“合约业务善意”。
